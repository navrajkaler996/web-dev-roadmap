import React from "react";
import { StyleSheet, View } from "react-native";

const RoadmapScreen = () => {
  return <View id="roadmap-container"></View>;
};

const roadmapStyles = StyleSheet.create({
  container: {
    height: "100",
  },
});

export default RoadmapScreen;
